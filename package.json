{
  "name": "trpc-demo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel --stream",
    "build": "lerna run build --stream",
    "list": "lerna ls --all"
  },
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
